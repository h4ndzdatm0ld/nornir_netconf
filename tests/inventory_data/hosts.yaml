---
netconf1.no_group:
  hostname: "netconf1.no_group"
  password: "netconf"
  port: 65025
  username: "netconf"
  connection_options:
    netconf:
      extras:
        allow_agent: false
        hostkey_verify: false
        look_for_keys: false
        ssh_config: null

netconf2.no_group:
  hostname: "localhost"
  password: "netconf"
  port: 65025
  username: "netconf"

netconf3.no_group:
  hostname: "netconf3.no_group"
  password: "netconf"
  port: 65024
  username: "netconf"
  connection_options:
    netconf:
      extras:
        allow_agent: false
        hostkey_verify: false
        look_for_keys: false
        ssh_config: null
